using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Analyzer.Types.ConfigurationTests
{
    [TestClass]
    public class ConfigurationTests
    {
        //Maybe you can mock the configuration objects to run the JSON engine with that and validate the config features?
        //Some end-to-end test cases with config files would be ideal too, it could work similar to the built-in rule tests
        //https://github.com/Azure/template-analyzer/tree/main/src/Analyzer.Core.BuiltInRuleTests

        [TestMethod]
        public void TestMethod1()
        {
        }
    }
}
